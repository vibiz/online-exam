{% extends 'ExamWebBundle::layout.html.twig' %}

{% block content %}
    <div class="row">
        <h4>Select Package:</h4>
        <table>
            <thead>
                <tr>
                    <th>Package Name</th>
                    <th>Exam Status</th>
                    <th>Last Time Left</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
            {% if all %}
                {% for enrollment in all %}
                <tr>
                    <td>{{ enrollment.package.name }}</td>
                    <td>{{ enrollment.startedOn and (enrollment.finishedOn == null) ? 'Progress' : 'Not Taken' }}</td>
                    <td><span class="label alert label-full">{{ enrollment.timeleft }}</span></td>
                    <td><a href="/exam/{{ enrollment.id }}" target="_self" onclick="return confirm('Start Exam?');" class="button tiny radius no-margin">Start Exam</a></td>
                </tr>
                {% endfor %}
             {% else %}
                <tr style="text-align: center">
                    <td colspan="4">No Package</td>
                </tr>
             {% endif %}
            </tbody>
        </table>
    </div>
{% endblock %}